<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BluetoothManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="BluetoothManager">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <!-- Настраиваем событие SelectionChanged для обработки кликов по меню -->
        <NavigationView x:Name="MainNav" 
                        IsSettingsVisible="False" 
                PaneDisplayMode="Left"
               
                OpenPaneLength="260"
                        SelectionChanged="MainNav_SelectionChanged" >
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavDevices" Tag="devices">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE702;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Uid="NavAnalyzer" Tag="analyzer">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB4A;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <!--Временная кнопка автоматического захвата экрана-->
            <!--<NavigationView.PaneFooter>
                <Button Content="📸 Захват для Store" Click="CaptureScreenshot_Click" Margin="10" Style="{StaticResource AccentButtonStyle}"/>
            </NavigationView.PaneFooter>-->

            <!-- Сюда динамически подгружаются наши страницы -->
            <Frame x:Name="ContentFrame" Padding="18"/>
            
        </NavigationView>
    </Grid>
</Window>
